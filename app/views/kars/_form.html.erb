<%= form_for(@kar) do |f| %>
  <% if @kar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kar.errors.count, "error") %> prohibited this kar from being saved:</h2>

      <ul>
      <% @kar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= collection_select :kar, :location_id,  @locations, :id, :title, {:prompt => 'Sila pilih'}%>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <p></p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
